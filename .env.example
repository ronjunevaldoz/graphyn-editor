# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.
#
# Get your Doppler token from:
#   Service token: Doppler dashboard → project → Access → Service Tokens
#   Personal token: doppler.com → Account → API → Personal Tokens
DOPPLER_TOKEN=dp.st.xxxxxxxxxxxxxxxxxxxx
